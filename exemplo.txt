Objetivo:
    Criar um API para controle de produtos.

    Model:
          Funcionário: id, nomeCompleto, cpf, função, salario

    Repository:
          obterTodos, obterPorId, adicionar, atualizar, deletar

    Service:
          obterTodos, obterPorId, adicionar, atualizar, deletar

    Controller:
          obterTodos, obterPorId, adicionar, atualizar, deletar
